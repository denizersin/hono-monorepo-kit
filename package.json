{
  "name": "my-mono-repo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev --filter=!@repo/db --filter=!@repo/config --filter=!@repo/logger",
    "dev-mobile": "turbo dev --filter=!@repo/db --filter=!@repo/config --filter=!@repo/logger --filter=!web",
    "dev2": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "check-types": "turbo check-types",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "bun@1.1.42",
  "devDependencies": {
    "turbo": "^2.3.3",
    "@types/react": "19.0.8",
    "@types/react-dom": "19.0.3"
  }
}